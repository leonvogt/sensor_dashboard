<div class="flex justify-center container mx-auto my-auto w-screen h-screen items-center flex-col">
  <div class="w-full md:w-3/4 lg:w-1/2 flex flex-col bg-slate-50 rounded-md p-10 gap-5">
    <div class="flex justify-end">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <%= f.hidden_field :email, value: User::GUEST_EMAIL %>
        <%= f.hidden_field :password, value: User::GUEST_PASSWORD %>
        <%= f.submit t('.sign_in_as_guest'), class: 'btn btn-secondary btn-sm w-full' %>
      <% end %>
    </div>

    <div class="text-center">
      <svg class="w-10 h-10 mx-auto pb-3" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path></svg>
      <div class="pb-3">Welcome back!</div>

      <div class="flex justify-center">
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="mb-6">
            <%= f.email_field :email, class: 'w-full py-4 px-8 bg-slate-200 placeholder:font-semibold rounded hover:ring-1 hover:ring-gray-600 outline-slate-500 border-solid border-2 border-slate-300', autofocus: true, placeholder: "email" %>
          </div>
          <div class="mb-6">
            <%= f.password_field :password, class: 'w-full py-4 px-8 bg-slate-200 placeholder:font-semibold rounded hover:ring-1 hover:ring-gray-600 outline-slate-500  border-solid border-2 border-slate-300', placeholder: "password" %>
          </div>
          <%= f.submit t('.sign_in'), class: 'btn btn-primary w-full' %>
        <% end %>
      </div>
      <div class="mt-6 text-sm">
        <div class="flex flex-col gap-2 items-center">
          <%= link_to t('devise.passwords.new.forgot_your_password'), new_password_path(resource_name) %><br />
          <%= link_to t('devise.signup'), new_registration_path(resource_name) %><br />
        </div>
      </div>
    </div>
  </div>
</div>
