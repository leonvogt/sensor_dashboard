<%= turbo_stream.update 'sensors-tab' do %>
  <%= render partial: 'devices/sensors_tab', locals: { device: @sensor.device, sensors: @sensors, sensor_to_show: @sensor_to_show} %>
<% end %>

<%= turbo_stream.toast 'success', t('successful.messages.updated', model: Sensor.model_name.human) %>
